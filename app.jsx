{"content":"\n```jsx\nimport React, { useState, useRef, useEffect } from 'react';\nimport { createRoot } from 'react-dom/client';\nimport { WebsimSocket, useQuery } from '@websim/use-query';\n\nconst room = new WebsimSocket();\n\nfunction App() {\n    // State for inputs and UI\n    const [imageDataUrl, setImageDataUrl] = useState(null);\n    const [isWebcamOn, setIsWebcamOn] = useState(false);\n    const [generatedResult, setGeneratedResult] = useState(null);\n    const [isGenerating, setIsGenerating] = useState(false);\n    const [isSharing, setIsSharing] = useState(false);\n    const [showSharedMessage, setShowSharedMessage] = useState(false);\n\n    // State for character customization\n    const [bitStyle, setBitStyle] = useState('16-bit');\n    const [charClass, setCharClass] = useState('Warrior');\n    const [charRace, setCharRace] = useState('Human');\n    const [charArmor, setCharArmor] = useState('Leather Armor');\n    const [includeOriginal, setIncludeOriginal] = useState(true);\n\n    // Refs for DOM elements\n    const videoRef = useRef(null);\n    const canvasRef = useRef(null);\n\n    const handleStartWebcam = async () => {\n        try {\n            const stream = await navigator.mediaDevices.getUserMedia({ video: true });\n            if (videoRef.current) {\n                videoRef.current.srcObject = stream;\n            }\n            setIsWebcamOn(true);\n        } catch (err) {\n            console.error(\"Error accessing webcam: \", err);\n            alert(\"Could not access webcam. Please check permissions.\");\n        }\n    };\n\n    const handleStopWebcam = () => {\n        if (videoRef.current && videoRef.current.srcObject) {\n            videoRef.current.srcObject.getTracks().forEach(track => track.stop());\n        }\n        setIsWebcamOn(false);\n    };\n\n    const handleCapturePhoto = () => {\n        if (videoRef.current && canvasRef.current) {\n            const video = videoRef.current;\n            const canvas = canvasRef.current;\n            canvas.width = video.videoWidth;\n            canvas.height = video.videoHeight;\n            const context = canvas.getContext('2d');\n            context.drawImage(video, 0, 0, canvas.width, canvas.height);\n            const dataUrl = canvas.toDataURL('image/png');\n            setImageDataUrl(dataUrl);\n            handleStopWebcam();\n        }\n    };\n\n    const handleFileUpload = (event) => {\n        const file = event.target.files[0];\n        if (file) {\n            const reader = new FileReader();\n            reader.onload = (e) => {\n                setImageDataUrl(e.target.result);\n                if (isWebcamOn) handleStopWebcam();\n            };\n            reader.readAsDataURL(file);\n        }\n    };\n\n    const dataURLtoFile = (dataurl, filename) => {\n        let arr = dataurl.split(','), mime = arr[0].match(/:(.*?);/)[1],\n            bstr = atob(arr[1]), n = bstr.length, u8arr = new Uint8Array(n);\n        while(n--){\n            u8arr[n] = bstr.charCodeAt(n);\n        }\n        return new File([u8arr], filename, {type:mime});\n    }\n\n    const handleGenerate = async () => {\n        if (!imageDataUrl) {\n            alert(\"Please upload or capture a photo first!\");\n            return;\n        }\n        setIsGenerating(true);\n        setGeneratedResult(null);\n        setShowSharedMessage(false);\n\n        try {\n            const analysisCompletion = await websim.chat.completions.create({\n                messages: [\n                    {\n                        role: \"system\",\n                        content: `You are an expert prompt engineer for a pixel art AI generator. Your task is to analyze an image of a person and user-defined character traits to create a detailed, high-quality prompt. Analyze the user's photo to identify key features like gender expression (masculine, feminine), hair color, hairstyle, and notable features like glasses or facial hair. Combine these observations with the user's choices for style, race, class, and armor. Construct a single, cohesive prompt that describes a full-body character sprite. The prompt should be comma-separated and include keywords like \"pixel art\", \"2D game sprite\", \"full body character\", \"standing pose\", and \"white background\". Emphasize the art style (e.g., 'SNES-style 16-bit pixel art'). Respond ONLY with a JSON object in the format: { \"prompt\": \"your_generated_prompt_here\" }. Do not include any other text or explanation.`,\n                    },\n                    {\n                        role: \"user\",\n                        content: [\n                            {\n                                type: \"text\",\n                                text: `Generate a prompt based on these choices: - Style: ${bitStyle} - Race: ${charRace} - Class: ${charClass} - Armor: ${charArmor}`,\n                            },\n                            {\n                                type: \"image_url\",\n                                image_url: { url: imageDataUrl },\n                            },\n                        ],\n                    },\n                ],\n                json: true,\n            });\n\n            const result = JSON.parse(analysisCompletion.content);\n            const generatedPrompt = result.prompt;\n\n            const imageResult = await websim.imageGen({\n                prompt: generatedPrompt,\n                aspect_ratio: \"1:1\",\n                transparent: true,\n            });\n\n            setGeneratedResult({ url: imageResult.url, prompt: generatedPrompt });\n\n        } catch (error) {\n            console.error(\"Error during generation:\", error);\n            alert(\"An error occurred while generating the character. Please try again.\");\n        } finally {\n            setIsGenerating(false);\n        }\n    };\n\n    const handleShare = async () => {\n        if (!generatedResult) return;\n        setIsSharing(true);\n\n        try {\n            // 1. Upload the generated sprite\n            const spriteResponse = await fetch(generatedResult.url);\n            const spriteBlob = await spriteResponse.blob();\n            const spriteFile = new File([spriteBlob], \"sprite.png\", { type: \"image/png\" });\n            const sprite_url = await websim.upload(spriteFile);\n\n            // 2. Upload original image if included\n            let original_image_url = null;\n            if (includeOriginal && imageDataUrl) {\n                const originalFile = dataURLtoFile(imageDataUrl, 'original.png');\n                original_image_url = await websim.upload(originalFile);\n            }\n\n            // 3. Create database record\n            await room.collection('characters_v1').create({\n                sprite_url,\n                original_image_url,\n                prompt: generatedResult.prompt,\n                bit_style: bitStyle,\n                char_class: charClass,\n                char_race: charRace,\n                char_armor: charArmor,\n            });\n\n            setShowSharedMessage(true);\n\n        } catch (error) {\n            console.error(\"Error sharing character:\", error);\n            alert(\"There was an error sharing your character. Please try again.\");\n        } finally {\n            setIsSharing(false);\n        }\n    };\n\n    return (\n        <div id=\"app-container\">\n            <header>\n                <h1>Retro Character Creator</h1>\n                <p>Turn yourself into a pixel art hero!</p>\n            </header>\n\n            <main>\n                <div className=\"column\" id=\"input-column\">\n                    <h2>1. Provide Your Portrait</h2>\n                    <div id=\"image-source\">\n                        <video ref={videoRef} autoPlay playsInline style={{ display: isWebcamOn ? 'block' : 'none' }}></video>\n                        <canvas ref={canvasRef} style={{ display: 'none' }}></canvas>\n\n                        <div id=\"image-preview-container\" style={{ display: isWebcamOn ? 'none' : 'flex' }}>\n                            {imageDataUrl ? (\n                                <img id=\"image-preview\" src={imageDataUrl} alt=\"Your image preview\" style={{display: 'block'}} />\n                            ) : (\n                                <p id=\"preview-placeholder\">Your photo will appear here</p>\n                            )}\n                        </div>\n\n                        <div className=\"button-group\">\n                            {!isWebcamOn ? (\n                                <button onClick={handleStartWebcam}>Start Webcam</button>\n                            ) : (\n                                <button onClick={handleCapturePhoto}>Capture Photo</button>\n                            )}\n                            <label htmlFor=\"upload-btn\" className=\"button\">Upload Photo</label>\n                            <input type=\"file\" id=\"upload-btn\" accept=\"image/*\" style={{ display: 'none' }} onChange={handleFileUpload} />\n                        </div>\n                    </div>\n\n                    <h2>2. Customize Your Character</h2>\n                    <div id=\"customization-options\">\n                        <div className=\"form-group\">\n                            <label htmlFor=\"bit-style\">Bit Style</label>\n                            <select id=\"bit-style\" value={bitStyle} onChange={e => setBitStyle(e.target.value)}>\n                                <option value=\"16-bit\">16-bit</option>\n                                <option value=\"8-bit\">8-bit</option>\n                                <option value=\"32-bit\">32-bit</option>\n                            </select>\n                        </div>\n                        <div className=\"form-group\">\n                            <label htmlFor=\"char-class\">Class</label>\n                            <select id=\"char-class\" value={charClass} onChange={e => setCharClass(e.target.value)}>\n                                <option value=\"Warrior\">Warrior</option>\n                                <option value=\"Mage\">Mage</option>\n                                <option value=\"Archer\">Archer</option>\n                                <option value=\"Rogue\">Rogue</option>\n                                <option value=\"Paladin\">Paladin</option>\n                                <option value=\"Necromancer\">Necromancer</option>\n                            </select>\n                        </div>\n                        <div className=\"form-group\">\n                            <label htmlFor=\"char-race\">Race</label>\n                            <select id=\"char-race\" value={charRace} onChange={e => setCharRace(e.target.value)}>\n                                <option value=\"Human\">Human</option>\n                                <option value=\"Elf\">Elf</option>\n                                <option value=\"Dwarf\">Dwarf</option>\n                                <option value=\"Orc\">Orc</option>\n                                <option value=\"Undead\">Undead</option>\n                                <option value=\"Gnome\">Gnome</option>\n                            </select>\n                        </div>\n                        <div className=\"form-group\">\n                            <label htmlFor=\"char-armor\">Armor/Equipment</label>\n                            <select id=\"char-armor\" value={charArmor} onChange={e => setCharArmor(e.target.value)}>\n                                <option value=\"Leather Armor\">Leather Armor</option>\n                                <option value=\"Iron Plate Armor\">Iron Plate Armor</option>\n                                <option value=\"Mystic Robes\">Mystic Robes</option>\n                                <option value=\"Adventurer's Gear\">Adventurer's Gear</option>\n                                <option value=\"Shadowy Cloak\">Shadowy Cloak</option>\n                            </select>\n                        </div>\n                    </div>\n                    <button id=\"generate-btn\" onClick={handleGenerate} disabled={isGenerating}>\n                        {isGenerating ? 'Generating...' : '3. Generate Character!'}\n                    </button>\n                </div>\n\n                <div className=\"column\" id=\"output-column\">\n                    <h2>Your Character Sprite</h2>\n                    <div id=\"result-container\">\n                        {isGenerating && (\n                            <div id=\"loading-indicator\">\n                                <div className=\"spinner\"></div>\n                                <p>Generating your character...</p>\n                            </div>\n                        )}\n                        {generatedResult && <img id=\"result-image\" src={generatedResult.url} alt=\"Generated character sprite\" />}\n                    </div>\n                    {generatedResult && (\n                        <>\n                            <div id=\"prompt-container\">\n                                <h3>Generated Prompt:</h3>\n                                <p id=\"prompt-text\">{generatedResult.prompt}</p>\n                            </div>\n                            <div id=\"share-container\">\n                                <div className=\"form-group-inline\">\n                                    <input type=\"checkbox\" id=\"include-original\" checked={includeOriginal} onChange={e => setIncludeOriginal(e.target.checked)} />\n                                    <label htmlFor=\"include-original\">Include original portrait</label>\n                                </div>\n                                <button id=\"share-btn\" onClick={handleShare} disabled={isSharing || showSharedMessage}>\n                                    {isSharing ? 'Sharing...' : (showSharedMessage ? 'Shared!' : 'Share to Gallery')}\n                                </button>\n                            </div>\n                        </>\n                    )}\n                </div>\n            </main>\n            <Gallery />\n        </div>\n    );\n}\n\nfunction Gallery() {\n    const { data: characters, loading } = useQuery(\n        room.query('SELECT * FROM public.characters_v1 ORDER BY created_at DESC LIMIT 20')\n    );\n    const [selectedChar, setSelectedChar] = useState(null);\n\n    return (\n        <section id=\"gallery-section\">\n            <h2>Community Gallery</h2>\n            {loading && <p>Loading gallery...</p>}\n            <div id=\"gallery-grid\">\n                {characters?.map(char => (\n                    <div key={char.id} className=\"gallery-item\" onClick={() => setSelectedChar(char)}>\n                        <img src={char.sprite_url} alt=\"Character sprite\" loading=\"lazy\" />\n                        <div className=\"gallery-item-overlay\">\n                            <p>@{char.username}</p>\n                        </div>\n                    </div>\n                ))}\n            </div>\n            {selectedChar && <GalleryModal character={selectedChar} onClose={() => setSelectedChar(null)} />}\n        </section>\n    );\n}\n\nfunction GalleryModal({ character, onClose }) {\n    return (\n        <div className=\"modal-backdrop\" onClick={onClose}>\n            <div className=\"modal-content\" onClick={e => e.stopPropagation()}>\n                <button className=\"modal-close-btn\" onClick={onClose}>X</button>\n                <div className=\"modal-body\">\n                    <div className=\"modal-images\">\n                        <div className=\"modal-image-container\">\n                            <h3>Generated Sprite</h3>\n                            <img src={character.sprite_url} alt=\"Generated Sprite\" />\n                        </div>\n                        {character.original_image_url && (\n                            <div className=\"modal-image-container\">\n                                <h3>Original Portrait</h3>\n                                <img src={character.original_image_url} alt=\"Original Portrait\" />\n                            </div>\n                        )}\n                    </div>\n                    <div className=\"modal-details\">\n                        <p><strong>Created by:</strong> @{character.username}</p>\n                        <p><strong>Style:</strong> {character.bit_style}</p>\n                        <p><strong>Race:</strong> {character.char_race}</p>\n                        <p><strong>Class:</strong> {character.char_class}</p>\n                        <p><strong>Armor:</strong> {character.char_armor}</p>\n                        <p className=\"prompt-details\"><strong>Prompt:</strong> {character.prompt}</p>\n                    </div>\n                </div>\n            </div>\n        </div>\n    );\n}\n\nconst root = createRoot(document.getElementById('root'));\nroot.render(<App />);"}